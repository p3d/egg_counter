<%= simple_form_for(@egg) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.input :uses %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
